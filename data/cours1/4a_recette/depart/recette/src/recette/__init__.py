# Ce fichier fait de `recette/` un paquet Python : sans lui, `import recette`
# ne trouve rien. Il ne contient aucun code — les fonctions sont dans les
# modules voisins, `calculs.py` pour les calculs et `tableau.py` pour la
# mise en forme du résultat.
