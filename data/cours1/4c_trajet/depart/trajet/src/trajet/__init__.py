# Ce fichier fait de `trajet/` un paquet Python : sans lui, `import trajet` ne
# trouve rien. Il ne contient aucun code — les fonctions sont dans les modules
# voisins, `etapes.py` pour la lecture du CSV, `images.py` pour le dessin des
# images, `montage.py` pour les sous-titres et l'assemblage de la vidéo.
