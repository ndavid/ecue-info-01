# Train

(À compléter : une phrase qui dit ce que fait le programme.)

## Installation

Le programme demande Python, ImageMagick et ffmpeg, décrits dans
`environment.yml`.

1. Installer Anaconda ou Miniforge.
2. Récupérer ce dossier (à compléter : par une archive, ou par `git clone`
   et l'adresse du dépôt).
3. Dans un terminal (Git Bash ou Anaconda Prompt), dans le dossier du
   projet :

```
conda env create -f environment.yml
conda activate animation
```

## Utilisation

Dans le dossier du projet, l'environnement `animation` activé.

(À compléter : pour chacune des trois fonctionnalités, une image, une série
d'images et une vidéo, la commande à taper et ce qu'elle écrit dans
`sortie/`.)

`python train.py --help` affiche toutes les options.

## Auteur

(À compléter.)
